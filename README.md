# Copilot

Dieses Repository enthält eine OpenWrt-Firmware-Vorlage für die **Creality Box / WB01**,
optimiert als sichere Cloud↔Heimnetz-Bridge mit Allowlist (z. B. nur 4 bekannte Server).

- Einstieg: `wb01-firmware/README.md`
- Build-Skript: `wb01-firmware/scripts/build_wb01.sh`
